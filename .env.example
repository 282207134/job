# OneSignal：控制台 https://onesignal.com → 应用 → Keys & IDs → App ID（REST API Key 仅放在 Cloud Functions 环境变量，勿提交仓库）
ONESIGNAL_APP_ID=
# 复制本文件为 .env 后填写（与 pubspec 中 assets 的 .env 一致，可直接编辑根目录 .env）
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
